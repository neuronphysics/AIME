# Minecraft. Deliberately scaffolding only.
#
# DreamerV3's Minecraft result is 100M env steps x 10 seeds, and the headline
# metric is "fraction of runs that ever collect a diamond". SHS-RSSM disables
# torch.compile, and measured throughput on Meta-World proprio was 0.57x the
# baseline. Budget this domain honestly before starting.
minecraft_shs:
  task: minecraft_diamond
  steps: 1e8
  parallel: True
  envs: 16
  eval_episode_num: 0
  eval_every: 1e4
  action_repeat: 1
  train_ratio: 16
  video_pred_log: true
  dyn_hidden: 1024
  dyn_deter: 4096
  units: 1024
  encoder: {mlp_keys: 'inventory|inventory_max|equipped|health|hunger|breath|obs_reward', cnn_keys: 'image', cnn_depth: 96, mlp_layers: 5, mlp_units: 1024}
  decoder: {mlp_keys: 'inventory|inventory_max|equipped|health|hunger|breath', cnn_keys: 'image', cnn_depth: 96, mlp_layers: 5, mlp_units: 1024}
  actor: {layers: 5, dist: 'onehot', std: 'none'}
  critic: {layers: 5}
  reward_head: {layers: 5}
  cont_head: {layers: 5}
  imag_gradient: 'reinforce'
  break_speed: 100.0
  time_limit: 36000
  dyn_discrete: 0
  use_shs: True
  shs_K: 16
  shs_init_scale: 0.01
  shs_shared_carry: True
  shs_q_rank: 4
  shs_recurrent: True
  shs_move_warmup: 5000
