# ProcGen. UNTESTED -- envs/procgen.py has never been run and the `procgen`
# package is not in requirements.txt. Scaffolding only.
#
# DreamerV3's ProcGen numbers use ONE seed per game, which makes it a noisy
# comparison target in both directions. Match distribution_mode and num_levels
# to the DreamerV3 appendix before claiming anything.
procgen_baseline:
  task: 'procgen_coinrun'
  steps: 25e6
  envs: 8
  action_repeat: 1
  train_ratio: 64
  time_limit: 10000
  video_pred_log: true
  procgen_distribution_mode: 'easy'
  procgen_num_levels: 0
  actor: {dist: 'onehot', std: 'none'}
  imag_gradient: 'reinforce'
  encoder: {mlp_keys: '$^', cnn_keys: 'image'}
  decoder: {mlp_keys: '$^', cnn_keys: 'image'}

procgen_shs:
  task: 'procgen_coinrun'
  steps: 25e6
  envs: 8
  action_repeat: 1
  train_ratio: 64
  time_limit: 10000
  video_pred_log: true
  procgen_distribution_mode: 'easy'
  procgen_num_levels: 0
  actor: {dist: 'onehot', std: 'none'}
  imag_gradient: 'reinforce'
  encoder: {mlp_keys: '$^', cnn_keys: 'image'}
  decoder: {mlp_keys: '$^', cnn_keys: 'image'}
  dyn_discrete: 0
  use_shs: True
  shs_K: 12
  shs_init_scale: 0.01
  shs_shared_carry: True
  shs_q_rank: 4
  shs_recurrent: True
  shs_move_warmup: 2000
