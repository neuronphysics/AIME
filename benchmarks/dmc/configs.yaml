# DMC lives in the root configs.yaml (dmc_proprio, dmc_vision, and the five
# dmc_*_shs task configs). This folder holds only the missing control arms.
#
# WARNING: dyn_discrete: 0 + use_shs: False is a BROKEN code path in this repo
# (see benchmarks/metaworld/configs.yaml, metaworld_proprio_gauss). These two
# configs exercise it. Verify actor_grad_norm is not ~1e-6 before using them.
dmc_vision_gauss:
  steps: 1e6
  action_repeat: 2
  envs: 4
  train_ratio: 512
  video_pred_log: true
  encoder: {mlp_keys: '$^', cnn_keys: 'image'}
  decoder: {mlp_keys: '$^', cnn_keys: 'image'}
  dyn_discrete: 0
  use_shs: False

dmc_proprio_gauss:
  steps: 5e5
  action_repeat: 2
  envs: 4
  train_ratio: 512
  video_pred_log: false
  encoder: {mlp_keys: '.*', cnn_keys: '$^'}
  decoder: {mlp_keys: '.*', cnn_keys: '$^'}
  dyn_discrete: 0
  use_shs: False
