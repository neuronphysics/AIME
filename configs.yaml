defaults:

  logdir: null
  traindir: null
  evaldir: null
  offline_traindir: ''
  offline_evaldir: ''
  seed: 0
  deterministic_run: False
  steps: 1e6
  parallel: False
  eval_every: 1e4
  eval_episode_num: 10
  log_every: 1e4
  reset_every: 0
  device: 'cuda:0'
  compile: True
  precision: 32
  debug: False
  video_pred_log: True

  task: 'dmc_walker_walk'
  size: [64, 64]
  envs: 1
  action_repeat: 2
  time_limit: 1000
  grayscale: False
  prefill: 2500
  reward_EMA: True

  dyn_hidden: 512
  dyn_deter: 512
  dyn_stoch: 32
  dyn_discrete: 32
  dyn_rec_depth: 1
  dyn_mean_act: 'none'
  dyn_std_act: 'sigmoid2'
  dyn_min_std: 0.1

  use_shs: False
  shs_K: 16
  shs_proj_dim: 64
  shs_gamma: 5.0
  shs_alpha: 8.0
  shs_kappa: 50.0
  shs_ema_tau: 0.02
  shs_teacher: True           # slow encoder reference for move scoring
  shs_teacher_tau: 0.005      # ~200-update horizon
  shs_teacher_drift_tol: 0.05
  shs_ema_memory_env: null    # optional: EMA memory in ENV steps (overrides shs_ema_tau)
  shs_recurrent: True
  # Gate persistence prior: E[pi_jj] = (kappa + alpha/K)/(alpha + kappa) for
  # Dir(alpha*beta + kappa*I), with kappa from the last non-recurrent phase.
  shs_prior_persist: 0.871   # = (50 + 8/16)/(8 + 50)
  shs_pg_iters: 4
  shs_rstick_dim: 32
  shs_rstick_stopgrad: True
  shs_rstick_weight_var: 1.0
  shs_rstick_bias_var: 4.0    # prior var of gate logit bias; small = strongly persistent
  # Hierarchical (disentangled) prior on the gate bias: r_j ~ N(mu, 1/lam) with
  # mu ~ N(logit(prior_persist), rstick_bias_var), lam ~ Gamma(a0, b0). E[mu] is
  # the population persistence, 1/E[lam] its spread across regimes; both are
  # learned by conjugate updates after every gate refit. False = fixed prior.
  shs_rstick_hier: True
  shs_rstick_hier_a0: 2.0
  shs_rstick_hier_b0: null     # null -> a0 * rstick_bias_var (same start as the fixed prior)
  shs_rstick_hier_used_only: False   # True: hyper-update ignores rows without gate evidence
  shs_rstick_hier_min_evidence: 1.0
  shs_live_moves: True         # False = never run scheduled live sweeps, so K stays at shs_K
  shs_ckpt_buffer: 8           # most-recent move-buffer batches stored in the checkpoint (fp16)
                               # so a resumed run sweeps on the same step; 0 = do not store
  shs_posterior_samples: 4     # sequential posterior draws averaged in the global-statistics
                               # update (regime M-step, transition counts, gate PG stats);
                               # 1 = single draw.  Loss, actor and move buffer are unaffected.
  shs_start_alpha: 8.0
  shs_v0_scale: 1.0
  shs_hdp_iters: 2
  shs_global_update_every: 1
  shs_move_every: 0
  shs_move_warmup: 2000
  shs_move_warmup_env: null   # optional: warmup in ENV steps (overrides shs_move_warmup)
  shs_move_birth: True
  shs_move_split: True
  shs_move_confirm_top: null
  shs_delete_mode: hughes
  shs_merge_select: hughes    # hughes = entropy-free complete shortlist (NIPS'15 Sec.4); residual = fast log-det ranking
  shs_merge_passes: 3
  shs_move_buffer: 8
  shs_move_max_age: 0
  shs_online_pairwise: False
  shs_imag_mode: 'actor_moment'
  shs_global_source: replay_ema
  shs_repr_epoch_steps: 0
  shs_ckpt_reservoir: False
  shs_stream_episodes: False
  shs_consolidate_every_episodes: 0
  shs_consolidate_warmup: 100000
  shs_consolidate_batches: 8
  shs_consolidate_sweeps: 3
  shs_consolidate_ep_len: 500
  shs_stream_chunked: True
  shs_imag_sample_mixture: True
  shs_analytic_estep: True
  shs_moment_consistent: True    # E-step, dynamics_kl AND move scoring condition on the
                                 # SAMPLED z_{t-1} that the recognition GRU consumed
                                 # (Rao-Blackwellised sequential-posterior statistics;
                                 # no predecessor-variance term).  False = analytic
                                 # independent-marginals approximation (mean + shifted
                                 # variance at t-1), the pre-v4 default.
                                 # and flips sign as the posterior sharpens.
  shs_gate_input: latent       # 'carry' = phi(h_t) (shipped); 'latent' = phi(z_{t-1})
  shs_gate_inner_iters: 1     # EXPERIMENTAL when > 1 (gate_input=latent only): each extra pass
                              # refits q(beta) on the current batch, re-runs the chain and then
                              # restores the persistent gate.  The result is optimal for that
                              # temporary gate, not for the persistent one, so the reported
                              # normaliser is not comparable and the posterior-draw batching is
                              # no longer an exact rearrangement.  Keep 1 unless experimenting.
  shs_inference_mode: filtered
  shs_chunk_boundary_mask: True
  shs_q_rank: 0
  shs_shared_carry: True
  shs_free: 1.0
  shs_ard: False
  shs_hdp_every: 1
  shs_pg_every: 1
  shs_strict_stream: False
  shs_stream_local_iters: 1
  shs_stream_discount: 1.0
  shs_a0: 3.0
  shs_b0: 2.0
  shs_rstick_use_action: True
  shs_calibrate_b0: True
  shs_calibrate_sF: 1.0
  shs_learn_b0: False
  shs_b0_strength: 2.0
  shs_action_dim: 0
  shs_merge_topm: null
  shs_online_mode: ema
  shs_expected_batches: null
  shs_strict_elbo: False
  shs_corpus_batches: null
  shs_entropy_mode: bounds
  shs_moves_complete: False
  shs_birth_style: seqcreate
  shs_diag_log: False
  shs_diag_figures: True
  # create_bonus set to 0.0 uniformly for cross-task comparability;
  # restore per-phase values to re-enable exploration-biased births.
  shs_curriculum: []

  grad_heads: ['decoder', 'reward', 'cont']
  units: 512
  act: 'SiLU'
  norm: True
  encoder:
    {mlp_keys: '$^', cnn_keys: 'image', act: 'SiLU', norm: True, cnn_depth: 32, kernel_size: 4, minres: 4, mlp_layers: 5, mlp_units: 1024, symlog_inputs: True}
  decoder:
    {mlp_keys: '$^', cnn_keys: 'image', act: 'SiLU', norm: True, cnn_depth: 32, kernel_size: 4, minres: 4, mlp_layers: 5, mlp_units: 1024, cnn_sigmoid: False, image_dist: mse, vector_dist: symlog_mse, outscale: 1.0}
  actor:
    {layers: 2, dist: 'normal', entropy: 3e-4, unimix_ratio: 0.01, std: 'learned', min_std: 0.1, max_std: 1.0, temp: 0.1, lr: 3e-5, eps: 1e-5, grad_clip: 100.0, outscale: 1.0}
  critic:
    {layers: 2, dist: 'symlog_disc', slow_target: True, slow_target_update: 1, slow_target_fraction: 0.02, lr: 3e-5, eps: 1e-5, grad_clip: 100.0, outscale: 0.0}
  reward_head:
    {layers: 2, dist: 'symlog_disc', loss_scale: 1.0, outscale: 0.0}
  cont_head:
    {layers: 2, loss_scale: 1.0, outscale: 1.0}
  dyn_scale: 0.5
  rep_scale: 0.1
  kl_free: 1.0
  weight_decay: 0.0
  unimix_ratio: 0.01
  initial: 'learned'

  batch_size: 16
  batch_length: 64
  train_ratio: 512
  pretrain: 100
  model_lr: 1e-4
  opt_eps: 1e-8
  grad_clip: 1000
  dataset_size: 1000000
  opt: 'adam'

  discount: 0.997
  discount_lambda: 0.95
  imag_horizon: 15
  imag_gradient: 'dynamics'
  imag_gradient_mix: 0.0
  eval_state_mean: False

  expl_behavior: 'greedy'
  expl_until: 0
  expl_extr_scale: 0.0
  expl_intr_scale: 1.0
  disag_target: 'stoch'
  disag_log: True
  disag_models: 10
  disag_offset: 1
  disag_layers: 4
  disag_units: 400
  disag_action_cond: False

shs_strict:
  use_shs: True
  dyn_discrete: 0
  shs_strict_elbo: True
  shs_free: 0.0
  kl_free: 0.0
  dyn_scale: 1.0
  rep_scale: 0.0
  shs_ard: False
  shs_q_rank: 0
  shs_curriculum: []
  shs_corpus_batches: 1
  # NOTE: memoized mode REQUIRES a stable batch id per replay partition.
  # Nothing in this repository calls SHSRSSM.set_batch_id(), so this preset
  # raises on the second queued global update by design (RegimeHead.
  # update_globals refuses batch_id=None rather than silently appending a
  # new batch on every replay revisit).  Wire set_batch_id() to your replay
  # partition id -- and set shs_expected_batches -- before using shs_strict.
  shs_online_mode: memoized
  shs_global_source: offline_memoized
  shs_strict_stream: True
  shs_move_every: 0
  shs_rstick_stopgrad: False

dmc_proprio:
  steps: 5e5
  action_repeat: 2
  envs: 4
  train_ratio: 512
  video_pred_log: false
  encoder: {mlp_keys: '.*', cnn_keys: '$^'}
  decoder: {mlp_keys: '.*', cnn_keys: '$^'}

dmc_vision:
  steps: 1e6
  action_repeat: 2
  envs: 4
  train_ratio: 512
  video_pred_log: true
  encoder: {mlp_keys: '$^', cnn_keys: 'image'}
  decoder: {mlp_keys: '$^', cnn_keys: 'image'}

crafter:
  task: crafter_reward
  step: 1e6
  action_repeat: 1
  envs: 1
  train_ratio: 512
  video_pred_log: true
  dyn_hidden: 1024
  dyn_deter: 4096
  units: 1024
  encoder: {mlp_keys: '$^', cnn_keys: 'image', cnn_depth: 96, mlp_layers: 5, mlp_units: 1024}
  decoder: {mlp_keys: '$^', cnn_keys: 'image', cnn_depth: 96, mlp_layers: 5, mlp_units: 1024}
  actor: {layers: 5, dist: 'onehot', std: 'none'}
  value: {layers: 5}
  reward_head: {layers: 5}
  cont_head: {layers: 5}
  imag_gradient: 'reinforce'

atari100k:
  steps: 4e5
  envs: 1
  action_repeat: 4
  train_ratio: 1024
  video_pred_log: true
  eval_episode_num: 100
  actor: {dist: 'onehot', std: 'none'}
  imag_gradient: 'reinforce'
  stickey: False
  lives: unused
  noops: 30
  resize: opencv
  actions: needed
  time_limit: 108000

# ---------------------------------------------------------------------------
# SHS-RSSM on Atari100k.  Compose as:
#   python dreamer.py --configs atari100k atari100k_shs atari_breakout_shs --logdir ...
# atari100k supplies the protocol (4e5 env frames = 100k agent steps at repeat 4,
# train_ratio 1024 -> 1 grad step per env frame, minimal action sets, 30 no-ops,
# onehot actor with REINFORCE); atari100k_shs swaps the transition prior; the
# per-game presets only set the task.  Baseline arm: --configs atari100k (unchanged).
atari100k_shs:
  dyn_discrete: 0            # SHS-RSSM is continuous-latent only
  use_shs: True
  shs_K: 16                  # a handful of game modes (play / death / respawn / wave);
                             # merge/delete prune, births add
  shs_action_dim: -1         # inherit config.num_actions: one-hot width of the game's
                             # MINIMAL action set (breakout 4, demon_attack 6,
                             # ms_pacman 9, alien / seaquest / hero 18)
  shs_proj_dim: 256
  shs_shared_carry: True
  shs_q_rank: 4
  shs_recurrent: True
  shs_rstick_use_action: True    # the gate reads a_{t-1}: FIRE / a direction change switches mode
  shs_rstick_hier: True
  shs_kappa: 10.0            # only read in recurrent:False phases (none below)
  shs_prior_persist: 0.9     # at repeat 4, modes last tens of agent steps; adapts
  shs_rstick_dim: 32
  shs_rstick_stopgrad: True
  shs_ema_tau: 0.01

  shs_b0: 0.2
  shs_calibrate_b0: True     # calibrated at the end of phase 1 (40k env frames)
  shs_calibrate_sF: 0.1

  shs_move_warmup: 0
  shs_move_buffer: 64
  shs_move_max_age: 2000
  shs_curriculum:
    # 400k env frames total; grad/env = 1.0, so *_env == gradient steps here
    - {until_env: 40000,  move_every: 0,           recurrent: True, birth: False, split: False, create_bonus: 0.0}
    - {until_env: 120000, move_every_env: 10000,   recurrent: True, birth: False, split: False, move_threshold: 10.0, create_bonus: 0.0}
    - {until_env: -1,     move_every_env: 10000,   recurrent: True, birth: True,  split: True,  move_threshold: 10.0, create_bonus: 0.0}
  shs_diag_log: True
  shs_diag_figures: True
  video_pred_log: True
  actor: {dist: 'onehot', std: 'none'}
  imag_gradient: 'reinforce'

atari_breakout_shs:
  task: 'atari_breakout'
atari_demon_attack_shs:
  task: 'atari_demon_attack'
atari_ms_pacman_shs:
  task: 'atari_ms_pacman'
atari_alien_shs:
  task: 'atari_alien'
atari_seaquest_shs:
  task: 'atari_seaquest'
atari_hero_shs:
  task: 'atari_hero'

minecraft:
  task: minecraft_diamond
  step: 1e8
  parallel: True
  envs: 16
  eval_episode_num: 0
  eval_every: 1e4
  action_repeat: 1
  train_ratio: 16
  video_pred_log: true
  dyn_hidden: 1024
  dyn_deter: 4096
  units: 1024
  encoder: {mlp_keys: 'inventory|inventory_max|equipped|health|hunger|breath|obs_reward', cnn_keys: 'image', cnn_depth: 96, mlp_layers: 5, mlp_units: 1024}
  decoder: {mlp_keys: 'inventory|inventory_max|equipped|health|hunger|breath', cnn_keys: 'image', cnn_depth: 96, mlp_layers: 5, mlp_units: 1024}
  actor: {layers: 5, dist: 'onehot', std: 'none'}
  value: {layers: 5}
  reward_head: {layers: 5}
  cont_head: {layers: 5}
  imag_gradient: 'reinforce'
  break_speed: 100.0
  time_limit: 36000

memorymaze:
  steps: 1e8
  action_repeat: 2
  actor: {dist: 'onehot', std: 'none'}
  imag_gradient: 'reinforce'
  task: 'memorymaze_9x9'

debug:
  debug: True
  pretrain: 1
  prefill: 1
  batch_size: 10
  batch_length: 20

# shared SHS block
# grad steps = steps / action_repeat / (batch_size*batch_length/train_ratio) = steps/16
# kappa is read ONLY in recurrent:False phases; the gate zeroes it when recurrent:True
# prior_persist is matched to kappa/(alpha+kappa) so the handoff is continuous
dmc_walker_shs:
  # Noise prior b0 0.2 / calibrate_sF 0.1 (the regimes are expected to explain most of
  # the innovation variance; sF=1.0 makes one wide regime a sufficient fit).  Carry
  # projection 256 of the 512 GRU units, low-rank noise 8, K 24, EMA tau 0.01.  The
  # recurrent hierarchical gate is on from step 0; merge/delete from 100k env steps
  # (first sweep ~104k), births/splits from 400k.  alpha 8 / prior_persist 0.9.
  task: 'dmc_walker_walk'
  steps: 1e6                 # curriculum *_env keys are ENV steps (batch-invariant)
  action_repeat: 2
  time_limit: 1000
  envs: 4

  dyn_discrete: 0
  use_shs: True
  shs_K: 24                  # over-provision; merge/delete prune down
  shs_action_dim: 6
  shs_proj_dim: 256          # carry map C reads 256 of the 512 GRU units (was 64)

  shs_shared_carry: True
  shs_q_rank: 8
  shs_recurrent: True
  shs_rstick_hier: True      # hierarchical N(mu, 1/lam) prior on the gate bias
  shs_kappa: 10.0            # only read in recurrent:False phases (none below)
  shs_prior_persist: 0.9     # hyper-prior mean of the gate bias, logit(0.9); adapts
  shs_rstick_dim: 32
  shs_rstick_stopgrad: True
  shs_ema_tau: 0.01          # ~100-update memory for the sufficient statistics

  shs_b0: 0.2                # Gamma rate on regime precision until calibration
  shs_calibrate_b0: True     # calibrated at the end of curriculum phase 1 (100k env)
  shs_calibrate_sF: 0.1      # b0 <- sF (a0-1) Var(dz): regimes explain ~90% of innovation var

  shs_move_warmup: 0
  shs_move_buffer: 64
  shs_move_max_age: 2000

  shs_curriculum:
    # phase 1: recurrent gate on, no structural moves; b0 is calibrated at its end
    - {until_env: 100000, move_every: 0,              recurrent: True, birth: False, split: False, create_bonus: 0.0}
    # phase 2: merge/delete only -- prune the over-provisioned K while the encoder settles
    - {until_env: 400000, move_every_env: 20800,      recurrent: True, birth: False, split: False, move_threshold: 10.0, create_bonus: 0.0}
    # phase 3: full birth/split/merge/delete
    - {until_env: -1,     move_every_env: 20800,      recurrent: True, birth: True,  split: True,  move_threshold: 10.0, create_bonus: 0.0}
  shs_diag_log: True
  shs_diag_figures: True
  video_pred_log: True
  encoder: {mlp_keys: '$^', cnn_keys: 'image'}
  decoder: {mlp_keys: '$^', cnn_keys: 'image'}

dmc_hopper_shs:
  task: 'dmc_hopper_hop'
  steps: 1e6                 # curriculum *_env keys are ENV steps (batch-invariant)
  action_repeat: 2
  time_limit: 1000
  envs: 4
  dyn_discrete: 0
  use_shs: True
  shs_K: 16
  shs_action_dim: 4
  shs_shared_carry: True
  shs_q_rank: 4
  shs_recurrent: True
  shs_kappa: 50.0
  # Gate persistence prior: E[pi_jj] = (kappa + alpha/K)/(alpha + kappa) for
  # Dir(alpha*beta + kappa*I), with kappa from the last non-recurrent phase.
  shs_prior_persist: 0.9   # = (10 + 8/16)/(8 + 10)
  shs_rstick_dim: 32
  shs_rstick_stopgrad: True
  shs_move_warmup: 0
  shs_move_buffer: 64
  shs_move_max_age: 2000
  shs_calibrate_b0: True
  shs_calibrate_sF: 1.0
  shs_curriculum:
    - {until_env: 500000, move_every: 0,    recurrent: False, kappa: 10.0, birth: False, split: False, create_bonus: 0.0}
    - {until_env: 650000, move_every_env: 22400, recurrent: False, kappa: 10.0, birth: True,  split: True,  move_threshold: 10.0, create_bonus: 0.0}
    - {until_env: 800000, move_every_env: 22400, recurrent: True,               birth: True,  split: True,  move_threshold: 10.0, create_bonus: 0.0}
    - {until_env: -1,    move_every_env: 22400, recurrent: True,               birth: True,  split: True,  move_threshold: 10.0, create_bonus: 0.0}
  shs_diag_log: True
  shs_diag_figures: True
  video_pred_log: True
  encoder: {mlp_keys: '$^', cnn_keys: 'image'}
  decoder: {mlp_keys: '$^', cnn_keys: 'image'}

dmc_cheetah_shs:
  task: 'dmc_cheetah_run'
  steps: 1e6                 # curriculum *_env keys are ENV steps (batch-invariant)
  action_repeat: 2
  time_limit: 1000
  envs: 4
  dyn_discrete: 0
  use_shs: True
  shs_K: 16
  shs_action_dim: 6
  shs_shared_carry: True
  shs_q_rank: 4
  shs_recurrent: True
  shs_kappa: 50.0
  # Gate persistence prior: E[pi_jj] = (kappa + alpha/K)/(alpha + kappa) for
  # Dir(alpha*beta + kappa*I), with kappa from the last non-recurrent phase.
  shs_prior_persist: 0.9   # = (10 + 8/16)/(8 + 10)
  shs_rstick_dim: 32
  shs_rstick_stopgrad: True
  shs_move_warmup: 0
  shs_move_buffer: 64
  shs_move_max_age: 2000
  shs_calibrate_b0: True
  shs_calibrate_sF: 1.0
  shs_curriculum:
    - {until_env: 500000, move_every: 0,    recurrent: False, kappa: 10.0, birth: False, split: False, create_bonus: 0.0}
    - {until_env: 650000, move_every_env: 22400, recurrent: False, kappa: 10.0, birth: False, split: False, move_threshold: 10.0, create_bonus: 0.0}
    - {until_env: 800000, move_every_env: 22400, recurrent: True,               birth: False, split: False, move_threshold: 10.0, create_bonus: 0.0}
    - {until_env: -1,    move_every_env: 22400, recurrent: True,               birth: True,  split: True,  move_threshold: 10.0, create_bonus: 0.0}
  shs_diag_log: True
  shs_diag_figures: True
  video_pred_log: True
  encoder: {mlp_keys: '$^', cnn_keys: 'image'}
  decoder: {mlp_keys: '$^', cnn_keys: 'image'}

dmc_acrobot_shs:
  # sF 0.1 / b0 0.2 noise prior, carry projection 256, EMA tau 0.01, recurrent
  # hierarchical gate from step 0, merge/delete from 100k env steps, births from 400k.
  # Swing-up vs. balance are the two regimes; one actuator. v1 (below, sF 1.0, K 12, rank 2, proj 64, open-loop first phase) was not better than DreamerV3.
  task: 'dmc_acrobot_swingup'
  steps: 1e6
  action_repeat: 2
  time_limit: 1000
  envs: 4
  dyn_discrete: 0
  use_shs: True
  shs_K: 8
  shs_action_dim: 1
  shs_proj_dim: 256
  shs_shared_carry: True
  shs_q_rank: 2
  shs_recurrent: True
  shs_rstick_hier: True
  shs_kappa: 10.0            # only read in recurrent:False phases (none below)
  shs_prior_persist: 0.9
  shs_rstick_dim: 32
  shs_rstick_stopgrad: True
  shs_ema_tau: 0.01
  shs_b0: 0.2
  shs_calibrate_b0: True
  shs_calibrate_sF: 0.1
  shs_move_warmup: 0
  shs_move_buffer: 64
  shs_move_max_age: 2000
  shs_curriculum:
    - {until_env: 100000, move_every: 0,          recurrent: True, birth: False, split: False, create_bonus: 0.0}
    - {until_env: 400000, move_every_env: 20800,  recurrent: True, birth: False, split: False, move_threshold: 10.0, create_bonus: 0.0}
    - {until_env: -1,     move_every_env: 20800,  recurrent: True, birth: True,  split: True,  move_threshold: 10.0, create_bonus: 0.0}
  shs_diag_log: True
  shs_diag_figures: True
  video_pred_log: True
  encoder: {mlp_keys: '$^', cnn_keys: 'image'}
  decoder: {mlp_keys: '$^', cnn_keys: 'image'}

dmc_pendulum_shs:
  # sF 0.1 / b0 0.2 noise prior, carry projection 256, EMA tau 0.01, recurrent
  # hierarchical gate from step 0, merge/delete from 100k env steps, births from 400k.
  # Canonical two-regime system (swing / balance), sparse reward; DreamerV3 has failure seeds here.
  task: 'dmc_pendulum_swingup'
  steps: 1e6
  action_repeat: 2
  time_limit: 1000
  envs: 4
  dyn_discrete: 0
  use_shs: True
  shs_K: 8
  shs_action_dim: 1
  shs_proj_dim: 256
  shs_shared_carry: True
  shs_q_rank: 2
  shs_recurrent: True
  shs_rstick_hier: True
  shs_kappa: 10.0            # only read in recurrent:False phases (none below)
  shs_prior_persist: 0.9
  shs_rstick_dim: 32
  shs_rstick_stopgrad: True
  shs_ema_tau: 0.01
  shs_b0: 0.2
  shs_calibrate_b0: True
  shs_calibrate_sF: 0.1
  shs_move_warmup: 0
  shs_move_buffer: 64
  shs_move_max_age: 2000
  shs_curriculum:
    - {until_env: 100000, move_every: 0,          recurrent: True, birth: False, split: False, create_bonus: 0.0}
    - {until_env: 400000, move_every_env: 20800,  recurrent: True, birth: False, split: False, move_threshold: 10.0, create_bonus: 0.0}
    - {until_env: -1,     move_every_env: 20800,  recurrent: True, birth: True,  split: True,  move_threshold: 10.0, create_bonus: 0.0}
  shs_diag_log: True
  shs_diag_figures: True
  video_pred_log: True
  encoder: {mlp_keys: '$^', cnn_keys: 'image'}
  decoder: {mlp_keys: '$^', cnn_keys: 'image'}

dmc_finger_turn_hard_shs:
  # sF 0.1 / b0 0.2 noise prior, carry projection 256, EMA tau 0.01, recurrent
  # hierarchical gate from step 0, merge/delete from 100k env steps, births from 400k.
  # Contact / no-contact with the spinner; sparse-ish reward.
  task: 'dmc_finger_turn_hard'
  steps: 1e6
  action_repeat: 2
  time_limit: 1000
  envs: 4
  dyn_discrete: 0
  use_shs: True
  shs_K: 12
  shs_action_dim: 2
  shs_proj_dim: 256
  shs_shared_carry: True
  shs_q_rank: 4
  shs_recurrent: True
  shs_rstick_hier: True
  shs_kappa: 10.0            # only read in recurrent:False phases (none below)
  shs_prior_persist: 0.9
  shs_rstick_dim: 32
  shs_rstick_stopgrad: True
  shs_ema_tau: 0.01
  shs_b0: 0.2
  shs_calibrate_b0: True
  shs_calibrate_sF: 0.1
  shs_move_warmup: 0
  shs_move_buffer: 64
  shs_move_max_age: 2000
  shs_curriculum:
    - {until_env: 100000, move_every: 0,          recurrent: True, birth: False, split: False, create_bonus: 0.0}
    - {until_env: 400000, move_every_env: 20800,  recurrent: True, birth: False, split: False, move_threshold: 10.0, create_bonus: 0.0}
    - {until_env: -1,     move_every_env: 20800,  recurrent: True, birth: True,  split: True,  move_threshold: 10.0, create_bonus: 0.0}
  shs_diag_log: True
  shs_diag_figures: True
  video_pred_log: True
  encoder: {mlp_keys: '$^', cnn_keys: 'image'}
  decoder: {mlp_keys: '$^', cnn_keys: 'image'}

dmc_quadruped_walk_shs:
  # sF 0.1 / b0 0.2 noise prior, carry projection 256, EMA tau 0.01, recurrent
  # hierarchical gate from step 0, merge/delete from 100k env steps, births from 400k.
  # Gait phases across four legs; camera id 2 is selected automatically by envs/dmc.py for the quadruped domain.
  # 12-dim action: the gate and the regime action term B_k a_{t-1} both read it.
  task: 'dmc_quadruped_walk'
  steps: 1e6
  action_repeat: 2
  time_limit: 1000
  envs: 4
  dyn_discrete: 0
  use_shs: True
  shs_K: 24
  shs_action_dim: 12
  shs_proj_dim: 256
  shs_shared_carry: True
  shs_q_rank: 8
  shs_recurrent: True
  shs_rstick_hier: True
  shs_kappa: 10.0            # only read in recurrent:False phases (none below)
  shs_prior_persist: 0.9
  shs_rstick_dim: 32
  shs_rstick_stopgrad: True
  shs_ema_tau: 0.01
  shs_b0: 0.2
  shs_calibrate_b0: True
  shs_calibrate_sF: 0.1
  shs_move_warmup: 0
  shs_move_buffer: 64
  shs_move_max_age: 2000
  shs_curriculum:
    - {until_env: 100000, move_every: 0,          recurrent: True, birth: False, split: False, create_bonus: 0.0}
    - {until_env: 400000, move_every_env: 20800,  recurrent: True, birth: False, split: False, move_threshold: 10.0, create_bonus: 0.0}
    - {until_env: -1,     move_every_env: 20800,  recurrent: True, birth: True,  split: True,  move_threshold: 10.0, create_bonus: 0.0}
  shs_diag_log: True
  shs_diag_figures: True
  video_pred_log: True
  encoder: {mlp_keys: '$^', cnn_keys: 'image'}
  decoder: {mlp_keys: '$^', cnn_keys: 'image'}

dmc_humanoid_shs:
  task: 'dmc_humanoid_walk'
  steps: 5e6                 # 312,500 grad steps
  action_repeat: 2
  time_limit: 1000
  envs: 4
  dyn_discrete: 0
  use_shs: True
  shs_K: 16                # 21 actuators, many distinct gait/recovery modes
  shs_action_dim: 21
  shs_shared_carry: True
  shs_q_rank: 4
  shs_recurrent: True
  shs_kappa: 50.0
  # Gate persistence prior: E[pi_jj] = (kappa + alpha/K)/(alpha + kappa) for
  # Dir(alpha*beta + kappa*I), with kappa from the last non-recurrent phase.
  shs_prior_persist: 0.9   # = (10 + 8/16)/(8 + 10)
  shs_rstick_dim: 32
  shs_rstick_stopgrad: True
  shs_move_warmup: 0
  shs_move_buffer: 64
  shs_move_max_age: 4000
  shs_calibrate_b0: True
  shs_calibrate_sF: 1.0
  shs_curriculum:
    - {until_env: 500000,  move_every: 0,    recurrent: False, kappa: 10.0, birth: False, split: False, create_bonus: 0.0}
    - {until_env: 650000, move_every_env: 128000, recurrent: False, kappa: 10.0, birth: False, split: False, move_threshold: 10.0, create_bonus: 0.0}
    - {until_env: 800000, move_every_env: 128000, recurrent: True,               birth: False, split: False, move_threshold: 10.0, create_bonus: 0.0}
    - {until_env: -1,     move_every_env: 128000, recurrent: True,               birth: True,  split: True,  move_threshold: 10.0, create_bonus: 0.0}
  shs_diag_log: True
  shs_diag_figures: True
  video_pred_log: True
  encoder: {mlp_keys: '$^', cnn_keys: 'image'}
  decoder: {mlp_keys: '$^', cnn_keys: 'image'}

procgen:
  task: 'procgen_coinrun'
  steps: 5e7
  action_repeat: 1          # procgen has none built in; 15 fps native
  envs: 8
  train_ratio: 512
  time_limit: 1000
  video_pred_log: true
  actor: {dist: 'onehot', std: 'none'}    # discrete: 15 actions
  imag_gradient: 'reinforce'
  encoder: {mlp_keys: '$^', cnn_keys: 'image'}
  decoder: {mlp_keys: '$^', cnn_keys: 'image'}

procgen_shs:
  # Hierarchical recurrent gate from step 0, carry projection 256, EMA tau 0.01,
  # b0 0.2 / sF 0.1.  Set shs_live_moves: False for fixed-K runs.
  # Generic procgen preset; override --task.
  task: 'procgen_coinrun'
  steps: 5e7
  action_repeat: 1          # procgen has none built in; 15 fps native
  envs: 8
  train_ratio: 512
  time_limit: 1000
  dyn_discrete: 0
  use_shs: True
  shs_K: 16
  shs_action_dim: 15        # one-hot width, all procgen games (Discrete(15))
  shs_proj_dim: 256
  shs_shared_carry: True
  shs_q_rank: 4
  shs_recurrent: True
  shs_rstick_use_action: True
  shs_rstick_hier: True
  shs_kappa: 10.0           # only read in recurrent:False phases (none below)
  shs_prior_persist: 0.9
  shs_rstick_dim: 32
  shs_rstick_stopgrad: True
  shs_ema_tau: 0.01
  shs_b0: 0.2
  shs_calibrate_b0: True    # calibrated at the end of phase 1 (200k env steps)
  shs_calibrate_sF: 0.1
  shs_move_warmup: 0
  shs_move_buffer: 64
  shs_move_max_age: 2000
  shs_curriculum:
    - {until_env: 200000,  move_every: 0,          recurrent: True, birth: False, split: False, create_bonus: 0.0}
    - {until_env: 1000000, move_every_env: 50000,  recurrent: True, birth: False, split: False, move_threshold: 10.0, create_bonus: 0.0}
    - {until_env: -1,      move_every_env: 50000,  recurrent: True, birth: True,  split: True,  move_threshold: 10.0, create_bonus: 0.0}
  actor: {dist: 'onehot', std: 'none'}
  imag_gradient: 'reinforce'
  encoder: {mlp_keys: '$^', cnn_keys: 'image'}
  decoder: {mlp_keys: '$^', cnn_keys: 'image'}
  shs_diag_log: True
  shs_diag_figures: True
  video_pred_log: True

procgen_bigfish_shs:
  # Hierarchical recurrent gate from step 0, carry projection 256, EMA tau 0.01,
  # b0 0.2 / sF 0.1.  Set shs_live_moves: False for fixed-K runs.
  # prey/threat/navigate modes last ~10-30 frames.
  task: 'procgen_bigfish'
  steps: 5e7
  action_repeat: 1          # procgen has none built in; 15 fps native
  envs: 8
  train_ratio: 512
  time_limit: 1000
  dyn_discrete: 0
  use_shs: True
  shs_K: 16
  shs_action_dim: 15        # one-hot width, all procgen games (Discrete(15))
  shs_proj_dim: 256
  shs_shared_carry: True
  shs_q_rank: 4
  shs_recurrent: True
  shs_rstick_use_action: True
  shs_rstick_hier: True
  shs_kappa: 10.0           # only read in recurrent:False phases (none below)
  shs_prior_persist: 0.9
  shs_rstick_dim: 32
  shs_rstick_stopgrad: True
  shs_ema_tau: 0.01
  shs_b0: 0.2
  shs_calibrate_b0: True    # calibrated at the end of phase 1 (200k env steps)
  shs_calibrate_sF: 0.1
  shs_move_warmup: 0
  shs_move_buffer: 64
  shs_move_max_age: 2000
  shs_curriculum:
    - {until_env: 200000,  move_every: 0,          recurrent: True, birth: False, split: False, create_bonus: 0.0}
    - {until_env: 1000000, move_every_env: 50000,  recurrent: True, birth: False, split: False, move_threshold: 10.0, create_bonus: 0.0}
    - {until_env: -1,      move_every_env: 50000,  recurrent: True, birth: True,  split: True,  move_threshold: 10.0, create_bonus: 0.0}
  actor: {dist: 'onehot', std: 'none'}
  imag_gradient: 'reinforce'
  encoder: {mlp_keys: '$^', cnn_keys: 'image'}
  decoder: {mlp_keys: '$^', cnn_keys: 'image'}
  shs_diag_log: True
  shs_diag_figures: True
  video_pred_log: True

procgen_fruitbot_shs:
  # Hierarchical recurrent gate from step 0, carry projection 256, EMA tau 0.01,
  # b0 0.2 / sF 0.1.  Set shs_live_moves: False for fixed-K runs.
  # corridor stretches between walls run longer than bigfish episodes.
  task: 'procgen_fruitbot'
  steps: 5e7
  action_repeat: 1          # procgen has none built in; 15 fps native
  envs: 8
  train_ratio: 512
  time_limit: 1000
  dyn_discrete: 0
  use_shs: True
  shs_K: 16
  shs_action_dim: 15        # one-hot width, all procgen games (Discrete(15))
  shs_proj_dim: 256
  shs_shared_carry: True
  shs_q_rank: 4
  shs_recurrent: True
  shs_rstick_use_action: True
  shs_rstick_hier: True
  shs_kappa: 15.0           # only read in recurrent:False phases (none below)
  shs_prior_persist: 0.674
  shs_rstick_dim: 32
  shs_rstick_stopgrad: True
  shs_ema_tau: 0.01
  shs_b0: 0.2
  shs_calibrate_b0: True    # calibrated at the end of phase 1 (200k env steps)
  shs_calibrate_sF: 0.1
  shs_move_warmup: 0
  shs_move_buffer: 64
  shs_move_max_age: 2000
  shs_curriculum:
    - {until_env: 200000,  move_every: 0,          recurrent: True, birth: False, split: False, create_bonus: 0.0}
    - {until_env: 1000000, move_every_env: 50000,  recurrent: True, birth: False, split: False, move_threshold: 10.0, create_bonus: 0.0}
    - {until_env: -1,      move_every_env: 50000,  recurrent: True, birth: True,  split: True,  move_threshold: 10.0, create_bonus: 0.0}
  actor: {dist: 'onehot', std: 'none'}
  imag_gradient: 'reinforce'
  encoder: {mlp_keys: '$^', cnn_keys: 'image'}
  decoder: {mlp_keys: '$^', cnn_keys: 'image'}
  shs_diag_log: True
  shs_diag_figures: True
  video_pred_log: True

dmc_vanilla_compare:
  # Upstream-faithful baseline arm: restores the three drifted defaults so
  # cross-repo curves share batch geometry, prefill, and recurrence depth.
  use_shs: False
  batch_size: 16
  prefill: 2500
  dyn_rec_depth: 1
