torch==2.4.1
matplotlib==3.5.0
ruamel.yaml==0.17.4
moviepy==1.0.3
einops==0.3.0
protobuf==3.20.0
gym==0.22.0
mujoco==2.3.5
dm_control==1.0.9
memory_maze==1.0.3
crafter==1.8.0
opencv-python==4.7.0.72
numpy==1.23.5
scikit-learn==1.3.2
tensorboard==2.17.1
# Meta-World benchmark (benchmarks/metaworld/). Pulls in gymnasium + mujoco>=3;
# the wrapper bridges gymnasium's 5-tuple step back to the old-gym API the
# rollout loop uses, so both gym and gymnasium coexist here intentionally.
metaworld
# ProcGen (benchmarks/procgen/) is NOT pinned here: it needs its own build
# toolchain and does not install cleanly alongside gym 0.22 on most setups.
# Install separately if you actually run that domain: pip install procgen
